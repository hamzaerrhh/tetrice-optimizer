package internal

import "fmt"

func Validate(text string) {

	fmt.Println("start validation")
	fmt.Println("step 1 validation")
	fmt.Println("calidate if there just blocks of 4 by 4 and contain just # adn .")
	fmt.Println("step 2")
	fmt.Println("validaet if there are conect (each one is conect even same x,same y or acrose) ")

}